{
  "name": "your-project-name",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "test:analytics": "node scripts/test-analytics.mjs",
    "check": "npm run build && node scripts/audit-site.mjs && npm run test:analytics"
  },
  "dependencies": {
    "astro": "^4.14.5",
    "esbuild": "^0.21.5"
  },
  "devDependencies": {
    "parse5": "^7.1.2"
  }
}
