{
  "name": "your-project-name",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "dev": "node scripts/dev-with-resume-watch.mjs",
    "start": "node scripts/dev-with-resume-watch.mjs",
    "generate:resume": "node scripts/generate-resume-pdf.mjs",
    "generate:cv": "npm run generate:resume",
    "prebuild": "npm run generate:resume",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^3.0.0"
  },
  "devDependencies": {}
}
